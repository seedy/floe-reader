import { styled } from "stitches.config";

export const Root = styled("p", {
  textDecoration: "line-through wavy $colors$default",
  overflow: 'hidden',
  overflowWrap: 'break-word',
  margin: 0,
});